import React from 'react';

const HomePage: React.FC = (): JSX.Element => {
    return (
        <div className="page">
            <h1 className="css-test">this is the home page </h1>
        </div>
    );
};

export default HomePage;
